import { pageMetadata } from "@/lib/seo-metadata";
export const metadata = pageMetadata({ title:"Terms & Conditions", description:"Read the LIDYA Jewellery website terms and conditions for enquiries, services, appointments and use of the website.", path:"/terms" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
