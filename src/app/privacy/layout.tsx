import { pageMetadata } from "@/lib/seo-metadata";
export const metadata = pageMetadata({ title:"Privacy Policy", description:"Read the LIDYA Jewellery privacy policy and information about personal data processing, cookies and client communications.", path:"/privacy" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
